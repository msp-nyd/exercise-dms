Found some errors that need to be fixed:
1. In dm.dimorders table, the column PAYMENT_METHOD_ID is declared as type text, it should be tinyint
2. In the ERD fact tables FK needs to be linked to there respective tables
